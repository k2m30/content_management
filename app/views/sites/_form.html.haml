= form_for @site, :html => { :class => 'form-horizontal' } do |f|
  .control-group
    = f.label 'Название', :class => 'control-label'
    .controls
      = f.text_field :name, :class => 'text_field'
  .control-group
    = f.label :'CSS', :class => 'control-label'
    .controls
      = f.text_field :css, :class => 'text_field'
  .control-group
    = f.label 'Код баннера', :class => 'control-label'
    .controls
      = f.text_area :banner, :class => 'text_area span8'
  .form-actions
    = f.submit 'Сохранить', :class => 'btn btn-primary'
    = link_to 'Назад', sites_path, :class => 'btn'
